/**
 * Shared types for the HushVoting Sovereign Shield design system.
 */

/* ── Button ── */

export type ButtonVariant = "primary" | "secondary" | "ghost";
export type ButtonSize = "sm" | "md" | "lg";

/* ── Card / Surface tones ── */

export type SurfaceTone = "default" | "primary" | "warning" | "error";

/* ── Status Chip ── */

export type StatusTone = "neutral" | "primary" | "warning" | "error";
