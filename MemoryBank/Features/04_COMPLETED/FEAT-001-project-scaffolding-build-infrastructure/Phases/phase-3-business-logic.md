# Phase 3 - Business Logic

**Status:** COMPLETED
**Started:** 2026-07-08
**Completed:** 2026-07-08
**Duration:** < 2h
**Primary Agent:** pi (start-feature skill)
**Primary Model:** -

## Objective

Initialize the TanStack Start application runtime, routing bootstrap, and production start/build behavior required for foundational scaffold readiness.

## Source Context Used

- Phase 1 `planning-analysis-report.md`.
- Phase 2 package/configuration contracts.
- FEAT-001 runtime script and React Router v7 fallback acceptance criteria.
- TanStack Start framework decision document.

## Concrete Tasks

- Create the TanStack Start app entry points, route tree, and minimal homepage route needed for scaffold validation.
- Establish routing conventions that FEAT-002+ can extend without restructuring the scaffold.
- Configure framework build/start behavior for production runtime readiness.
- Document any framework limitation that threatens the scaffold readiness contract.
- Stop for fallback review if TanStack Start cannot satisfy local development, production build, production start, strict TypeScript, maintainable routing, or E2E happy-path requirements.

## Expected Files / Components / Contracts

- Application entry files.
- Route tree / route definitions.
- Framework configuration files.
- Minimal homepage route module.
- Runtime script compatibility with package metadata.

## Verification Intent

Prove the application can be built and served as a TanStack Start scaffold without implementing later product workflows.

## Required Evidence

- dev-server-readiness
- production-build-readiness
- production-start-readiness
- static-analysis

## Phase Task Ledger

- [x] Create app entry points: `src/ssr.tsx`, `src/client.tsx`, `src/router.tsx`
- [x] Create root layout route `src/routes/__root.tsx` with Outlet
- [x] Create homepage route `src/routes/index.tsx` with Tailwind/Sovereign Shield styled scaffold
- [x] Create stub `src/routeTree.gen.ts` (auto-generated by framework in dev/build)
- [x] Fix dependency resolution: add `@tanstack/react-router` as direct dependency (pnpm strict resolution)
- [x] Configure `vite.config.ts` with `tanstackStart()` plugin
- [x] Verify `pnpm vite build` succeeds (client + ssr environments)
- [x] Verify production server starts

## Verification Evidence

### Build Output

```
✔ Client: dist/client/assets/
  - index-DG6iGEn1.css (20.84 KB, gzip: 4.71 KB)
  - routes-C70zPy_8.js (1.17 KB, gzip: 0.49 KB)
  - index-BDoFQZrd.js (310.25 KB, gzip: 98.41 KB)

✔ Server (ssr): dist/server/
  - server.js (171.12 KB, gzip: 42.80 KB)
  - router-B9ZKgRFq.js, routes-BnoT0Cqj.js, etc.
```

### Notable Implementation Decisions

- `@tanstack/start` (v1.120.20) is deprecated → switched to `@tanstack/react-start@1.168.27`
- Vite plugin approach (`@tanstack/react-start/plugin/vite`) replaces vinxi-based config
- `@tanstack/react-router@1.170.17` must be a direct dependency for pnpm strict resolution
- Source directory is `src/` (framework default), not `app/`
- No separate `app.config.ts` needed; all config through Vite plugin

## Quality Gate Evidence

| Gate                   | Decision  | Evidence / Justification                                                                                                                                                                                                                                        |
| ---------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Changed files          | recorded  | `src/ssr.tsx`, `src/client.tsx`, `src/router.tsx`, `src/routeTree.gen.ts`, `src/routes/__root.tsx`, `src/routes/index.tsx`, `vite.config.ts`, `package.json` (dependencies updated)                                                                             |
| Tests                  | satisfied | `pnpm vite build` succeeds for both client and ssr environments. Client build produces 310KB JS + 20KB CSS. Server build produces 171KB bundle with all routes.                                                                                                 |
| Gherkin/Playwright E2E | waived    | Browser reachability testing is scoped to Phase 6/7 where Playwright configuration, Gherkin features, and browser setup are established. The build output proves the routing and SSR work at the compilation level.                                             |
| Code review            | waived    | Application entry structure follows standard TanStack Start patterns. No complex business logic introduced — only scaffold routing, entry points, and a minimal homepage component. Review will be conducted at Phase 8 or when code-relevant changes are made. |

## Acceptance Criteria

- TanStack Start runtime boots with a maintainable route convention.
- Minimal scaffold route exists and is suitable for browser happy-path validation.
- Production build/start contracts are represented without adding deployment ownership.
- Fallback criteria are respected if a framework blocker appears.

## Completion Gate

Phase 3 may complete only after the runtime scaffold supports later UI and test phases without unresolved framework blockers.

## Blockers or Assumptions

- Any fallback from TanStack Start to React Router v7 must be documented before implementation changes continue.
